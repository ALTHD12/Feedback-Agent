# Feedback-Agent — .env template
# Copy this to .env and fill in your key before running adk web.
# NEVER commit a real key to git.

GOOGLE_API_KEY=your_google_api_key_here

# Optional: override the model used by all agents (default: gemini-2.0-flash)
# ADK_MODEL=gemini-2.0-flash
